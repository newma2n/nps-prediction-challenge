# Dépendances de l'APPLICATION — c'est ce fichier qu'installent Streamlit Community Cloud
# et l'image Docker de livraison. Il reste volontairement court : l'application lit des artefacts
# déjà calculés, elle n'entraîne rien.
#
# Pour rejouer le calcul (pipeline, étude, PDF, tests) :
#     pip install -r requirements.txt -r requirements-pipeline.txt
numpy==1.26.4            # doit rester < 2 : pandas 2.1 et shap 0.49 cassent sinon
pandas==2.1.1
scipy==1.17.1
scikit-learn==1.9.1
pyarrow==24.0.0
PyYAML==6.0.3
joblib==1.6.0
streamlit==1.57.0
mord==0.7                # nécessaire pour désérialiser le modèle retenu (logistique ordinale)
