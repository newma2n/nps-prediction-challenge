# Dépendances pour REJOUER le calcul (pipeline, étude, PDF, tests).
# À installer en plus de requirements.txt :  pip install -r requirements.txt -r requirements-pipeline.txt
#
# Elles ne sont volontairement PAS dans requirements.txt, parce que ce fichier est aussi celui
# qu'installe Streamlit Community Cloud : y laisser PyTorch et les modèles de fondation ferait
# dépasser la limite de ressources de l'hébergement pour des paquets dont l'application n'a
# aucun besoin — leurs résultats sont déjà calculés et lus dans reports/fondation.json.
matplotlib==3.8.0
seaborn==0.13.0
openpyxl==3.1.2
shap==0.49.1

# Catalogue de modèles (phase 4)
xgboost==2.1.4          # 2.1.x : la 3.x casse SHAP TreeExplainer en multi-classe
lightgbm==4.7.0
catboost==1.2.10
imbalanced-learn==0.14.2
sklearn-compat==0.1.6

# Modèles de fondation tabulaires (bonus) — torch CPU suffit
torch==2.14.0
tabicl==2.2.0
tabpfn==9.0.0           # poids sur dépôt Hugging Face à accès contrôlé (acceptation + huggingface-cli login)

# Verbatims par LLM local (bonus § 4.4) — Qwen2.5-1.5B-Instruct téléchargé depuis Hugging Face au premier lancement (~3 Go)
transformers==5.17.0
tokenizers==0.23.2
regex

# Write-up PDF, verbatims via API, notebook, captures, tests
markdown==3.10.3
pymupdf==1.28.0
anthropic==1.6.0
nbformat==5.11.1
nbclient==0.11.0
nbconvert==7.17.1
fastjsonschema==2.22.2
ipykernel==6.26.0
selenium==4.49.0
pytest==9.1.1
